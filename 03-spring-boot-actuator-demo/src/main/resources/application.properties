info.app.name=Spring Boot Actuator Demo
info.app.description=Demo Spring Boot Application for Spring Boot Actuator
info.app.version=1.0.0

#Use wildcard "*" to expose all end-points
#Can also expose individual end-points with a comma-delimited list:
#management.endpoints.web.exposure.include=health,info
management.endpoints.web.exposure.include=*
management.info.env.enabled=true

#Exclude individual end-points with a comma-delimited list:
#management.endpoints.web.exposure.exclude=health,info